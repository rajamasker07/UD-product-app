import { ValidationErrorPipe } from './validation-error.pipe';

describe('ValidationErrorPipe', () => {
  it('should be defined', () => {
    expect(new ValidationErrorPipe()).toBeDefined();
  });
});
